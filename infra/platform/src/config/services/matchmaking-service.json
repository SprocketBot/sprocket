{
  "transport": {{{transport}}},
  "redis": {
    "port": {{redis.port}},
    "host": "{{ redis.host }}",
    "prefix": "{{ redis.prefix }}"
  },
  "logger": {
    "levels": {{{logger.levels}}}
  }
}
