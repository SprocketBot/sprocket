{
  "client": {
    "gqlUrl": "{{ frontend.url }}",
    "secure": true,
    "chatwoot": {
      "enabled": false,
      "url": "https://chatwoot.sprocket.mlesports.gg",
      "websiteToken": "{{ chatwoot.websiteToken }}"
    }
  },

  "server": {},

  "stack": "{{ stack }}"
}
