{
  "bot": {
    "prefix": "{{ bot.prefix }}"
  },
  "transport": {{{transport}}},
  "minio": {
    "endPoint": "{{s3.endpoint}}",
    "port": {{ s3.port }},
    "useSSL": {{ s3.ssl }},
    "bucketNames": {
      "replays": "{{s3.buckets.replayParse}}",
      "image_generation": "{{s3.buckets.imageGeneration}}"
    }
  },
  "logger": {
    "levels": {{{logger.levels}}}
  },
  "gql": {
    "url": "http://{{ gql.internal }}"
  }
}
