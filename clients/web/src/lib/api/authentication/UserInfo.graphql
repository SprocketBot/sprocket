query UserInfo @cache(policy: NetworkOnly) {
    me @cache(policy: NetworkOnly) {
        name: displayName
        organizations {
            id
        }
        currentOrganization {
            name
            logoUrl
        }
    }
}
